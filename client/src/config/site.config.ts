export const siteConfig = {
    title: "Cosmetics",
    description: "You will get all type of cosmetics product here.",
    author: {},
    keywords: [],
};

export type SiteConfig = typeof siteConfig;
