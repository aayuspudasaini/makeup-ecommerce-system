const { Router } = require("express");
const { asyncHandler } = require("../middlewares/async-handler");
const bookroute = Router();


bookroute.get("/",);
bookroute.post("/submit",)

module.exports = bookroute;